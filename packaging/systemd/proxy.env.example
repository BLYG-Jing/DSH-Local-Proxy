# systemd EnvironmentFile syntax. This file is not sourced by a shell.
LISTEN_HOST=127.0.0.1
LISTEN_PORT=18082
UPSTREAM_HOST=127.0.0.1
UPSTREAM_PORT=18080
HISTORY_READ_TIMEOUT_MS=120000
WEBSOCKET_HANDSHAKE_TIMEOUT_MS=10000
UPSTREAM_STATE_TTL_MS=60000
RESPONSE_COMPRESSION=1
COMPRESSION_THRESHOLD_BYTES=1024
MAX_PATCHED_RESPONSE_BYTES=2097152
IMMUTABLE_STATIC_CACHE=1

# Base64 of the exact UTF-8 password. Generate without putting the password in
# shell history, for example: printf 'Password: ' >&2; read -rs P; echo >&2;
# printf %s "$P" | base64 | tr -d '\n'; unset P
AUTH_PASSWORD_B64=cmVwbGFjZS13aXRoLWEtbG9uZy1yYW5kb20tcGFzc3dvcmQ=
